const names = ["midlaj","pulladan","zack","midu" ]

const [fullname, innetion,shortname,nickname] = names;
// console.log(names[0]);

console.log(shortname);


const person ={
    id:0,
    name:"midlaj",
    age:90

    
}

// console.log(person.id);

const {name,id,age} = person;

console.log(age);
